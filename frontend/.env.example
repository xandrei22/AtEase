# Optional: backend API URL (defaults to same origin; Vite proxies /api in dev)
# VITE_API_URL=

# Google Sign-In (optional – Web client ID from Google Cloud Console)
# When set, the Customer Login page shows "Sign in with Google".
# Use the same Client ID in backend .env as GOOGLE_CLIENT_ID.
# VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
